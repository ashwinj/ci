void shifters() {
	int i;
	for(i = 1; i < 1024;) {
		i <<= 1;
		if(i % 128) continue;
		printf("%d\n", i);
	}
	for(; i >= 1;) {
		i >>= 1;
		if(i % 128 == 0) continue;
		printf("%d\n", i);
	}
}

int main() {
	shifters();
	return 0;
}
